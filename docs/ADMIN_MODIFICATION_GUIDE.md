# 관리자 수정 및 자동 배포 가이드

## 개요

협력사가 작성한 모든 항목(점검, 수리, 이관 등)을 관리자가 수정하고, 수정 이력을 관리하며, 승인 후 자동으로 협력사에 배포하는 시스템입니다.

---

## 주요 기능

### 1. 관리자 수정 (`admin_modifications`)

**수정 가능 항목:**
- 일상점검 (`daily_checks`)
- 정기점검 (`inspections`)
- 습합점검 (`fitting_checks`)
- 세척점검 (`cleaning_checks`)
- 수리요청 (`repairs`)
- 이관요청 (`transfer_logs`)
- 생산수량 입력
- 타수 기록 (`shots`)

**수정 유형:**
- `correction` - 오류 수정
- `enhancement` - 데이터 보완
- `data_quality` - 품질 개선
- `compliance` - 규정 준수

**심각도:**
- `minor` - 경미한 수정
- `major` - 중요한 수정
- `critical` - 치명적 수정 (승인 필수)

### 2. 수정 승인 (`modification_approvals`)

**승인 단계:**
- 1차: 팀장 승인
- 2차: 부서장 승인
- 3차: 본부장 승인 (critical만)

**검토 사항:**
- 데이터 정확성 검증
- 규정 준수 확인
- 영향도 평가

### 3. 자동 배포 (`auto_deployment`)

**배포 유형:**
- `immediate` - 즉시 배포 (승인 직후)
- `scheduled` - 예약 배포 (지정 시간)
- `batch` - 일괄 배포 (여러 건 묶음)

**배포 범위:**
- `single_user` - 특정 사용자
- `plant` - 특정 협력사
- `all_plants` - 전체 협력사
- `system_wide` - 시스템 전체

---

## 운영 프로세스

### 1. 관리자: 협력사 데이터 수정

**페이지**: `/admin/modifications/edit`

**수정 절차:**
```
1. 수정 대상 선택
   - 테이블: daily_checks
   - 레코드 ID: 12345
   
2. 수정 사유 입력
   "점검 결과 오류 수정 - 실제로는 '수리요'였으나 '양호'로 잘못 입력됨"
   
3. 수정 내용 입력
   - 필드: overall_status
   - 변경 전: "pass"
   - 변경 후: "fail"
   
4. 수정 유형 선택
   - 유형: correction
   - 심각도: major
   
5. 승인 필요 여부 선택
   - major 이상: 승인 필요
   - minor: 즉시 적용
   
6. 배포 설정
   - 배포 방법: immediate (즉시)
   - 배포 범위: single_user (원작성자)
   
7. 저장
```

**API 예시:**
```javascript
POST /api/admin/modifications

{
  "target_table": "daily_checks",
  "target_record_id": 12345,
  "mold_id": 789,
  "original_author_id": 45,
  "modification_reason": "점검 결과 오류 수정",
  "original_value": {
    "overall_status": "pass",
    "findings": "이상 없음"
  },
  "modified_value": {
    "overall_status": "fail",
    "findings": "게이트 부위 마모 발견"
  },
  "changes_summary": [
    {
      "field": "overall_status",
      "before": "pass",
      "after": "fail"
    },
    {
      "field": "findings",
      "before": "이상 없음",
      "after": "게이트 부위 마모 발견"
    }
  ],
  "modification_type": "correction",
  "severity": "major",
  "requires_approval": true,
  "deployment_method": "immediate",
  "deployment_scope": "single_user"
}
```

### 2. 승인자: 수정 승인

**페이지**: `/admin/modifications/approvals`

**승인 절차:**
```
1. 대기 중인 수정 목록 확인
2. 수정 내역 상세 확인
   - 원본 데이터
   - 수정 데이터
   - 변경 사항 비교
   - 수정 사유
3. 검토 사항 체크
   ☑ 데이터 정확성 검증
   ☑ 규정 준수 확인
   ☑ 영향도 평가
4. 승인/거부/조건부 승인 선택
5. 승인 의견 입력
6. 제출
```

**API 예시:**
```javascript
POST /api/admin/modifications/:id/approve

{
  "approval_level": 1,
  "approval_status": "approved",
  "approval_comments": "수정 내용 확인 완료. 승인합니다.",
  "data_accuracy_verified": true,
  "compliance_checked": true,
  "impact_assessed": true,
  "risk_level": "low"
}
```

### 3. 시스템: 자동 배포

**배포 트리거:**
- 승인 완료 시 자동 실행
- 예약 시간 도달 시 실행
- 수동 배포 버튼 클릭 시 실행

**배포 프로세스:**
```
1. 배포 준비
   - 배포 대상 확인
   - 배포 범위 확인
   - 배포 시간 확인
   
2. 배포 실행
   - 원본 데이터 백업
   - 수정 데이터 적용
   - 배포 진행률 업데이트
   
3. 알림 발송
   - 원작성자에게 알림
   - 관련 사용자에게 알림
   
4. 배포 완료
   - 배포 상태 업데이트
   - 배포 로그 기록
```

**자동 배포 로직:**
```javascript
async function autoDeployModification(modificationId) {
  const modification = await AdminModification.findByPk(modificationId);
  
  // 1. 배포 레코드 생성
  const deployment = await AutoDeployment.create({
    modification_id: modificationId,
    deployment_type: 'immediate',
    deployment_trigger: 'approval',
    target_scope: 'single_user',
    deployment_status: 'in_progress',
    stages: [
      { stage: 'validation', status: 'in_progress' },
      { stage: 'backup', status: 'pending' },
      { stage: 'deployment', status: 'pending' },
      { stage: 'notification', status: 'pending' }
    ]
  });
  
  try {
    // 2. 검증
    await validateModification(modification);
    await updateStage(deployment, 'validation', 'completed');
    
    // 3. 백업
    await backupOriginalData(modification);
    await updateStage(deployment, 'backup', 'completed');
    
    // 4. 배포 (실제 데이터 수정)
    await applyModification(modification);
    await updateStage(deployment, 'deployment', 'completed');
    
    // 5. 알림 발송
    await sendNotifications(modification);
    await updateStage(deployment, 'notification', 'completed');
    
    // 6. 완료 처리
    await deployment.update({
      deployment_status: 'completed',
      actual_deployment_time: new Date(),
      deployment_progress: 100,
      success_count: 1
    });
    
    await modification.update({
      is_deployed: true,
      deployed_at: new Date()
    });
    
  } catch (error) {
    // 에러 처리 및 롤백
    await deployment.update({
      deployment_status: 'failed',
      error_message: error.message,
      errors: [{ stage: deployment.current_stage, error: error.message }]
    });
    
    // 자동 롤백 시도
    if (deployment.retry_count < deployment.max_retries) {
      await retryDeployment(deployment);
    } else {
      await rollbackModification(modification);
    }
  }
}
```

### 4. 협력사: 수정 알림 확인

**알림 유형:**
- 📝 데이터 수정 알림
- ✅ 수정 승인 알림
- 🔄 배포 완료 알림

**알림 내용:**
```
┌─────────────────────────────────────┐
│ 📝 점검 데이터 수정 알림              │
│                                     │
│ 관리자가 귀하의 점검 데이터를         │
│ 수정했습니다.                        │
│                                     │
│ 금형: M-2024-001                    │
│ 점검일: 2024-01-15                  │
│                                     │
│ 수정 내용:                           │
│ • 점검 결과: 양호 → 수리요           │
│ • 발견사항: 게이트 부위 마모 발견     │
│                                     │
│ 수정 사유:                           │
│ 실제 점검 결과 재확인 후 수정         │
│                                     │
│ [수정 내역 확인] [이의 제기]          │
└─────────────────────────────────────┘
```

---

## 수정 이력 조회

### 관리자 화면

**페이지**: `/admin/modifications/history`

**조회 기능:**
- 기간별 조회
- 수정자별 조회
- 테이블별 조회
- 승인 상태별 조회
- 배포 상태별 조회

**표시 항목:**
| 수정일시 | 수정자 | 대상 | 수정 내용 | 승인 상태 | 배포 상태 |
|---------|--------|------|----------|----------|----------|
| 2024-01-15 10:30 | 김관리 | 일상점검 #12345 | 결과: 양호→수리요 | 승인 완료 | 배포 완료 |

### 협력사 화면

**페이지**: `/plant/modifications/my-data`

**조회 기능:**
- 내가 작성한 데이터의 수정 이력만 조회
- 수정 사유 확인
- 수정 전후 비교
- 이의 제기 가능

---

## 롤백 기능

### 롤백 조건
- 배포 후 24시간 이내
- 중대한 오류 발견 시
- 협력사 이의 제기 승인 시

### 롤백 프로세스
```
1. 롤백 요청
2. 롤백 사유 입력
3. 승인 (필요 시)
4. 원본 데이터 복원
5. 알림 발송
6. 롤백 완료
```

**API 예시:**
```javascript
POST /api/admin/modifications/:id/rollback

{
  "rollback_reason": "협력사 이의 제기 승인",
  "rolled_back_by": 10
}
```

---

## 대시보드 및 통계

### 관리자 대시보드

**페이지**: `/admin/modifications/dashboard`

**통계 항목:**
- 금일 수정 건수
- 대기 중인 승인 건수
- 배포 완료 건수
- 롤백 건수
- 수정 유형별 분포
- 협력사별 수정 빈도

**차트:**
- 월별 수정 추이
- 수정 유형별 비율
- 승인 소요 시간
- 배포 성공률

---

## API 엔드포인트

### 관리자

```
POST   /api/admin/modifications                    # 수정 생성
GET    /api/admin/modifications                    # 수정 목록
GET    /api/admin/modifications/:id                # 수정 상세
PUT    /api/admin/modifications/:id                # 수정 업데이트
DELETE /api/admin/modifications/:id                # 수정 삭제
POST   /api/admin/modifications/:id/approve        # 승인
POST   /api/admin/modifications/:id/reject         # 거부
POST   /api/admin/modifications/:id/rollback       # 롤백
GET    /api/admin/modifications/pending-approval   # 승인 대기 목록
GET    /api/admin/modifications/history            # 수정 이력
```

### 배포

```
POST   /api/admin/deployments/execute              # 수동 배포 실행
GET    /api/admin/deployments/:id                  # 배포 상세
GET    /api/admin/deployments/:id/status           # 배포 상태
POST   /api/admin/deployments/:id/retry            # 배포 재시도
POST   /api/admin/deployments/:id/rollback         # 배포 롤백
```

### 협력사

```
GET    /api/plant/modifications/my-data            # 내 데이터 수정 이력
GET    /api/plant/modifications/:id                # 수정 상세 확인
POST   /api/plant/modifications/:id/dispute        # 이의 제기
```

---

## 알림 설정

### 알림 수신 대상

**관리자:**
- 수정 생성 시
- 승인 요청 시
- 배포 완료 시
- 배포 실패 시
- 롤백 시

**협력사:**
- 데이터 수정 시
- 수정 승인 시
- 배포 완료 시
- 롤백 시

### 알림 발송 방법
- 시스템 알림 (notifications 테이블)
- 이메일 (선택)
- SMS (긴급 시)

---

## 보안 및 권한

### 수정 권한
- 본사 관리자만 수정 가능
- 부서별 권한 설정 가능
- 수정 이력 영구 보존

### 승인 권한
- 팀장: minor, major 승인
- 부서장: major, critical 승인
- 본부장: critical 최종 승인

### 롤백 권한
- 부서장 이상만 롤백 가능
- 롤백 사유 필수 입력
- 롤백 이력 영구 보존

---

## 데이터 흐름도

```
[협력사 데이터 작성]
  ↓
[DB 저장]
  ↓
[관리자 수정 필요 시]
  ↓
[admin_modifications 생성]
  - 원본 데이터 백업
  - 수정 내용 기록
  ↓
[승인 필요 여부 판단]
  ↓ (승인 필요)
[modification_approvals]
  - 1차 승인 (팀장)
  - 2차 승인 (부서장)
  - 3차 승인 (본부장, critical만)
  ↓
[승인 완료]
  ↓
[auto_deployment 트리거]
  - 배포 준비
  - 데이터 검증
  - 원본 백업
  - 수정 적용
  - 알림 발송
  ↓
[배포 완료]
  ↓
[협력사 알림 수신]
  - 수정 내역 확인
  - 이의 제기 가능
```

---

## 예시 시나리오

### 시나리오 1: 점검 결과 수정

**상황**: 협력사가 일상점검에서 "양호"로 입력했으나, 관리자가 현장 확인 결과 "수리요"로 판단

**프로세스**:
```
1. 관리자: 점검 데이터 수정
   - 대상: daily_checks #12345
   - 변경: overall_status "pass" → "fail"
   - 사유: "현장 재확인 결과 게이트 마모 발견"
   - 심각도: major
   
2. 시스템: 승인 요청 생성
   - 팀장에게 승인 요청 알림
   
3. 팀장: 승인
   - 수정 내용 검토
   - 승인 완료
   
4. 시스템: 자동 배포
   - 원본 데이터 백업
   - 수정 데이터 적용
   - 협력사에게 알림 발송
   
5. 협력사: 알림 확인
   - 수정 내역 확인
   - 수정 사유 확인
```

### 시나리오 2: 생산수량 수정

**상황**: 협력사가 생산수량을 잘못 입력 (5,000개 → 500개)

**프로세스**:
```
1. 관리자: 생산수량 수정
   - 대상: inspection_schedules
   - 변경: current_production_quantity 500 → 5000
   - 사유: "입력 오류 수정"
   - 심각도: minor
   
2. 시스템: 즉시 배포 (승인 불필요)
   - 수정 데이터 적용
   - 협력사에게 알림 발송
   
3. 협력사: 알림 확인
   - 수정 내역 확인
```

---

## 결론

이 시스템을 통해:
- ✅ 관리자가 협력사 데이터를 체계적으로 관리
- ✅ 모든 수정 이력이 영구 보존
- ✅ 승인 프로세스로 데이터 품질 보장
- ✅ 자동 배포로 신속한 데이터 반영
- ✅ 롤백 기능으로 오류 복구 가능
- ✅ 투명한 수정 이력 관리
